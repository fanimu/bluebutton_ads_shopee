# Default landing page when accessed with trailing slash
DirectoryIndex monitoring_polo.php index.php

# Block direct HTTP access to JSON files and Git files
<FilesMatch "\.(json|git|gitignore)$">
    Require all denied
</FilesMatch>
